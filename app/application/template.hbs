{{#unless noNav}}
  <nav class='navbar navbar-default'>

    <div class='container-fluid'>

      <div class='navbar-header'>
        {{#link-to 'index' classNames='navbar-brand'}}
          <img src='assets/images/brand-white.png'>
        {{/link-to}}
      </div>

      <ul class='nav navbar-nav navbar-right'>
      
        {{#if session.isAuthenticated}}
          <li>
            {{#link-to 'go'}}
              <i class='fa-lg fa-search'></i>
            {{/link-to}}
          </li>          <li>
            {{#link-to 'me.listings.add'}}
              <i class='fa-plus fa-lg'></i>
            {{/link-to}}
          </li>
          <li>
            {{#link-to 'me'}}
              <i class='fa-user fa-lg'></i>
              {{me.firstName}}
            {{/link-to}}
          </li>
        {{else}}
          <li>
            {{#link-to 'login'}}
              <i class='fa-sign-in fa-lg'></i>
            {{/link-to}}
          </li>
        {{/if}}

      </ul>

    </div>
  </nav>
{{/unless}}

<div class='container-fluid main-body'>
  {{outlet}}
</div>

{{#unless noFooter}}
  <div class='footer'>
    <div class='container-fluid'>
      <p class='text-center'>
        © {{year}} mYgration
      </p>
    </div>
  </div>
{{/unless}}

{{ember-notify messageStyle='bootstrap'}}